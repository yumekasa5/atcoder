#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<vector<int>> p(4, vector<int>(4));
    for (int i = 0; i < 4; i++)
    {
        for (int j = 0; j < 4; j++)
        {
            cin >> p[i][j];
        }
    }

    return 0;
}